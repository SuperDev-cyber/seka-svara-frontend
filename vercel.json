{
  "installCommand": "npm cache clean --force && npm install --no-audit --no-fund --legacy-peer-deps --prefer-online --registry=https://registry.npmjs.org/ --fetch-retries=10 || npm cache clean --force && npm install --no-audit --no-fund --legacy-peer-deps --ignore-scripts",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production",
    "NPM_CONFIG_REGISTRY": "https://registry.npmjs.org/",
    "NPM_CONFIG_NETWORK_CONCURRENCY": "1",
    "NPM_CONFIG_FETCH_TIMEOUT": "600000",
    "NPM_CONFIG_FETCH_RETRIES": "5",
    "NPM_CONFIG_FETCH_RETRY_FACTOR": "2",
    "NPM_CONFIG_FETCH_RETRY_MINTIMEOUT": "10000",
    "NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT": "600000",
    "NPM_CONFIG_FUND": "false",
    "NPM_CONFIG_AUDIT": "false"
  }
}
