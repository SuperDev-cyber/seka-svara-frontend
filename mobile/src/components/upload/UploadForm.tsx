// Stub - Upload features disabled (Supabase removed)
type UploadFormProps = {
  onSuccess?: () => void;
};

export function UploadForm(_props: UploadFormProps) {
  return (
    <div className="p-4 text-muted-foreground">
      Upload features are not available in the mobile app.
    </div>
  );
}
